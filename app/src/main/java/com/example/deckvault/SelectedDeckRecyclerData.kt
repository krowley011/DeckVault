package com.example.deckvault

data class SelectedDeckRecyclerData (
    var cardImage: String ? = "",
    var cardName: String ? = "",
    var cardSubName: String ? = "",
    var cardNumber: Int ? = 0
)
